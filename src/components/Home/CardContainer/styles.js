import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  mainContainer: {
    marginTop: "50px",
    marginLeft: "90px"
  },
  smMargin: {
    margin: theme.spacing(1),
  },
  actionDiv: {
    textAlign: 'center',
  },
  progress: {
    marginTop: "50px",
    marginBottom: "50px"
  }
}));